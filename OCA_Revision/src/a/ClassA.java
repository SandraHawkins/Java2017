/** 
 * Author: Sandra Hawkins
 * Date:   23 Aug 2017
 * OCA_Revision/a/ClassA.java
 */ 

package a;

/*
 * A class can be public or default only, not protected or private.
 * A default class can only be seen in the same package, it cannot
 * be imported/ instantiated in another package.
 */
class ClassA {
	
	/* name can be seen only in same package because the class
	 * has default access. */
	public String name = "ClassA";

}
